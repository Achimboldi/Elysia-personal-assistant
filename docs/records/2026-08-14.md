# 2026-08-14 工作日志

## 目标
为 Elysia 桌面版建立完整的文档体系（AGENTS.md + docs/），使后续每次更新有迹可循、新接手者能快速上手。

## 完成项

### 1. 建立文档体系
- 现象：项目缺乏统一文档结构，只有零散的 `LINUX交接文档.md`、`智能体优化方案_v1.md`、`.workbuddy/memory/` 日志，架构知识散落在各文件头注释和子代理分析报告中，不利于持续维护与交接。
- 根因：项目演进快（3.2 万行 JS + 记忆子系统），从未系统化沉淀架构文档。
- 修复（新建文件，均为纯文档，零代码改动）：
  - `AGENTS.md`：AI 协作入口（项目定位、硬红线、任务路由、完成标准）
  - `docs/README.md`：文档索引 + 阅读顺序
  - `docs/architecture/desktop-arch.md`：桌面端整体架构（进程模型/模块清单/页面结构）
  - `docs/architecture/data-model.md`：data.json 数据模型（字段/读写层/墓碑/去重/多用户）
  - `docs/architecture/ai-engine.md`：昔涟 AI 引擎（提示词结构/幻觉检测/兜底/工具集）
  - `docs/architecture/memory-system.md`：记忆子系统（MemoryConstellations 集成/数据流/注意点）
  - `docs/architecture/cloud-sync.md`：云同步合并算法（冲突/墓碑/竞态保护/坑位）
  - `docs/runbook.md`：运维手册（部署/启动/构建/双端同步/坑位/安全风险）
  - `docs/decisions/README.md`：ADR 决策记录模板
  - `docs/records/2026-08-14.md`：本日志
  - `CHANGELOG.md`：变更日志
- 验证：全部 Markdown 纯文档，无代码/数据改动；`node --check` 不适用（无 JS 改动）；文档内容基于源码与子代理分析报告核对。

### 2. 安全风险评估（只记录，未处理）
- 现象：发现多处敏感信息暴露风险（data.json 明文密钥、app.log 打印完整 data.json、移动端 initial_data.json 硬编码百度云凭证、~/.git-credentials PAT）。
- 决策：按用户确认，本轮只记录不处理，风险清单已写入 `docs/runbook.md` 第九节，待后续授权后整改。

## 遗留 / 待办
- [ ] 移动端仓库同步建立对应文档体系（已委托子代理执行）
- [ ] 桌面端文档同步到 `linux-app/resources/app`（双端一致）
- [ ] 提交 git 并推送（桌面端 + 移动端）
- [ ] 安全风险整改（凭证轮换/日志脱敏）待用户授权后处理
- [ ] 后续重要架构决策按 ADR 模板补充到 `docs/decisions/`

### 4. 修复：删除提醒后云同步复活问题（电脑端）
- 现象：用户在电脑端/手机端删除提醒任务后，提醒会在云同步后恢复出现。
- 根因（双端一致）：
  1. 删除提醒不写墓碑：`reminder-manager.js deleteReminder` 只从 settings.reminders splice，未记录 deletedItems.reminders
  2. 提醒合并无墓碑过滤：`mergeReminderLists`（main.js）/ `_mergeReminders`（手机端 agent_sync_helper）都是"云端独有保留"；云端残留的已删除提醒被当独有合并回本地 → 复活
  3. reminders 不在墓碑集合（_tombstoneCollections / applyDeletionTombstones collections）中
- 修复（电脑端，手机端按用户要求暂缓）：
  - `reminder-manager.js`：persistReminders 支持 deletedItemsOverride 参数；deleteReminder 删除时写墓碑 deletedItems.reminders
  - `main.js`：新增 filterRemindersByTombstone 辅助函数；applyDeletionTombstones 增加 reminders 墓碑过滤（过滤 mergedData.settings.reminders + combined.reminders 回传）；手动云下载路径 mergedSettings 后应用 filterRemindersByTombstone
- 覆盖路径：自动同步主 merge（_mergeWithConflictDetection→applyDeletionTombstones）、自动同步下载（_downloadCloudData→applyDeletionTombstones）、手动云下载（mergedSettings 单独过滤）
- 验证：node --check 双端通过；逻辑测试（墓碑过滤场景）通过；win-unpacked 与 linux-app 两目录文件已同步一致
- 遗留：历史已删除但云端仍残留的提醒，修复后首次同步仍可能复活一次（当时无墓碑），需再手动删除一次即可写入墓碑
