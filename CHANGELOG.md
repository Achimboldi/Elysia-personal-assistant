# Elysia 变更日志

> 按日期归纳的重要变更（详细四段式记录见 `docs/records/`）。格式：`## YYYY-MM-DD - 主题`，`### 变更` + `### 影响`。

## 2026-08-14 - 建立文档体系

### 变更
- 新增 `AGENTS.md`（AI 协作入口：项目定位、硬红线、任务路由）
- 新增 `docs/` 目录：文档索引、架构文档（桌面架构/数据模型/AI 引擎/记忆子系统/云同步）、运维手册（runbook）、ADR 模板、按日工作日志
- 新增 `CHANGELOG.md`（本文件）

### 影响
- 纯文档，零代码/数据改动，不影响任何功能
- 后续每次改动需按新规范在 `docs/records/` 记录并同步更新相关文档
